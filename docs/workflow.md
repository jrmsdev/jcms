* jcms
	* main
		* parse cmd flags
		* webapp config
		* create webapp
		* start
		* serve
		* stop
	* start
		* log init
		* webapp setup
		* webapp start
		* httpd listen (MISS)
	* serve
		* httpd serve (MISS)
	* stop
		* webapp stop
		* httpd stop (MISS)

## internal

* webapp
	* setup
		* setup assets manager
		* setup storage manager (MISS)
	* start
		* setup httpd handlers (MISS)
	* stop
